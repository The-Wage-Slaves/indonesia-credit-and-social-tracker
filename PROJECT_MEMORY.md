# 项目长期记忆（Codex / Claude Code 共用）

> 本文件保存跨会话不可丢失的项目决策。任何代理在修改方法论、自动化、看板或 GitHub
> 流程前必须完整阅读本文件、根目录 `AGENTS.md` / `CLAUDE.md` 及相关模块说明；不得只依赖
> 聊天上下文或压缩摘要。

## 1. 产品与单一真源

- 这是两个纯静态 HTML 看板及其自动取数链：消费信贷市场追踪与印尼稳定性指数。
- 正式、已确认的对外结果只来自 GitHub `main`；`bot/*` 只保存待人工确认的采集产物。
- 稳定性页面名称固定为「全景等权版」（内部 v3）与「数据置信版」（内部 v4）。禁止把
  v3/v4 恢复为面向读者的主标题。
- 稳定性正式评分数据单一真源为 `stability-monitor/dashboard/data.js`；信贷看板数据与
  逻辑单一真源为 `credit-tracker/dashboard/credit-dashboard.html`。

## 2. 方法论边界

- 人在环是铁律：采集器只抓取并写 pending；任何周度分数、正式历史或看板数据都必须经
  所有者明确确认后写入。
- 事实、引述、判断、待补必须分离；稳定性驱动项必须带 `updated`、`changeReason`、
  `sources`，且按权重重新验算。
- 数据置信版五支柱权重固定为财政25%、货币25%、政策与制度15%、社会25%、强制机构10%；
  低置信度事件型证据不得获得与官方统计相同的影响力。
- FX 换算参数固定 15000 以保持信贷历史可比；国家宏观板块的 USD/IDR 使用 BI JISDOR，
  两者不可联动修改。
- 信贷恐慌指数是近期波动信号，不是长期基本面评分。新闻密度、负面程度、社媒热度与
  负面率分别保留；缺失社媒覆盖必须标为 unavailable，不得当作平静或零分。
- 红色事件必须满足证据门：原始来源与至少两个独立来源，或方法论文档规定的跨新闻/社媒
  异常条件。模型翻译和分类是辅助判断，不能替代来源核验。

## 3. 自动化与密钥

- 日频、周频、月度采集全部在 GitHub Actions 云端运行，不依赖所有者电脑开机。
- 日频只在风险或采集异常时推送；周频为周二；月度为每月1日。例行数据不得创建 PR。
- `DEEPSEEK_API_KEY` 用于中文释义/分类，`YOUTUBE_API_KEY` 用于相关社媒覆盖，
  `BPS_API_KEY` 只用于月度 BPS 宏观数据；飞书使用 webhook 与签名 secret。
- 缺 key 或采集失败必须显式显示 unconfigured/failure，不得用旧结果冒充本次结果。
- OpenAI Private Sites 传输代码和 GitHub 的两个 Sites secrets 已于 PR #10 清除；托管平台上
  的旧 Site 项目本体仍待账户登录后永久删除，不得误称已经删除。

## 4. 看板交付

- 不安装开机启动服务器，不创建15分钟轮询，不要求本机正式 clone 才能查看。
- `main` 的看板相关文件更新后，由 GitHub Actions 覆盖同一 rolling Release 资产
  `indonesia-monitor-dashboard.zip`，并由飞书发送固定下载链接。
- 私有仓库下载需要 GitHub 登录和只读权限；没有权限的同事只能由所有者转发 ZIP。
- ZIP 必须能解压后直接打开 `index.html`，不得包含密钥、采集配置、`.git` 或源代码工具链。

## 5. PR纪律（强制）

- 一个用户目标只对应一个 PR。开始前先搜索现有开放 PR 和目标分支。
- 同一目标的代码、文档、测试及 CI 修复全部继续推送到原分支/原 PR；不得为补说明、补测试、
  修同一轮 bug 另开 PR。
- PR 在实现、静态检查、真实运行路径验证全部完成前保持 Draft，不得抢先合并。
- 只有所有者明确拆分独立范围，或原 PR 已合并后出现真正独立的紧急问题，才能新建 PR；
  此时必须先解释原因。PR 编号不可回收，不得用改写历史追求表面整洁。
- PR #10—#13 被拆成四个连续补丁是反例；以后不得重复。

## 6. 上下文防腐流程

1. 开始工作：读本文件、代理规则、`AUTOMATIONS.md`，再按任务读取
   `credit-tracker/PROJECT_BRIEF.md` 或稳定性方法论文档。
2. 修改前：确认单一真源、人在环边界、当前开放 PR 与未提交改动。
3. 修改后：跑仓库校验与对应业务测试；涉及工作流时验证真实触发路径，而不只验证语法。
4. 结束前：把新增长期决策同步到本文件和相关方法论文档；临时状态写 `HANDOFF.md`。


