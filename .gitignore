# 密钥（绝不入库——用 street_heat_config.example.yaml 作模板）
stability-monitor/scripts/street_heat_config.yaml
credit-tracker/sentiment-monitor/credit_sentiment_config.yaml

# 依赖与缓存
**/node_modules/
**/__pycache__/
*.pyc

# 运行产物（体积大/可再生；确认单与pending保留在库里以便"打开即最新"）
credit-tracker/p2p-scraper/results/debug_*.txt
credit-tracker/data/source/_Tabel_17_latest.xls

# 私人对话存档
credit-tracker/conversation_history.html

# 备份
*.bak-*
# 注: daily-events 证据池【不再 gitignore】——云端 daily-risk-alerts.yml 需要把它
# 提交到 bot/daily-risk-alerts 分支作为周更输入。被 ignore 会让 workflow 的
# `git add` 静默失败、判定「无变化」而永不上云。产物只进 bot 分支，不进 main。
